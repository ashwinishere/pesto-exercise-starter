
// Don't change the export values.
// You can use the function in the Math module
const sqrt = (input) => Math.sqrt(input);
const power = (baseValue, exponent) => baseValue ** exponent;
const round = (input) => Math.round(input);
export {
  sqrt,
  power,
  round,
};
